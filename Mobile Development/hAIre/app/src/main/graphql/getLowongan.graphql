query getLowongan($id: Int) {
    getLowongan(idlowongan: $id) {
        success
        errors
        lowongan {
            id
            nama
            jmlh_butuh
            deskripsi
            company_id
        }
    }
}