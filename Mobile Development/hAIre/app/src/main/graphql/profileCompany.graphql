query profileCompany($id: Int) {
    profileCompany(id: $id) {
        errors
        success
        company {
            id
            deskripsi
            nama
            url_photo
            alamat
        }
    }
}